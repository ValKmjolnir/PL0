program test;

const num0:=114514,num1:=1919810;
var x,y,t,loop;
procedure swap();
    begin
        t:=x;
        x:=y;
        y:=t
    end;
procedure add();
    begin
        x:=x+y
    end;
procedure sub();
    begin
        x:=x-y
    end
begin
    read(x,y);
    loop:=10;
    while loop>0 do
        begin
            call swap();
            call add();
            call sub();
            loop:=loop-1
        end;
    if odd x then
        begin
            write(0)
        end
    else
        begin
            write(1)
        end;
    write(-x+y*x-x*x/y)
end